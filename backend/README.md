后端结构
cs2-betting-backend/
├── .env                 # 配置文件 (存放 RPC 和合约地址)
├── .python-version      # uv 生成的版本文件
├── pyproject.toml       # uv 生成的依赖管理文件
├── uv.lock              # uv 生成的锁定文件
├── abi.json             # 合约接口文件 (复用之前的内容)
└── app.py               # 主后端代码